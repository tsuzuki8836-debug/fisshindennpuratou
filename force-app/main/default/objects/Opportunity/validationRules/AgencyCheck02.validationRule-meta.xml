<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>AgencyCheck02</fullName>
    <active>true</active>
    <description>「扱方：直扱・仕切」のときは「商談手数料」を入力できないように制限する</description>
    <errorConditionFormula>AND(
    OR(
        ISPICKVAL( Handling__c , &quot;直扱&quot;), 
        ISPICKVAL( Handling__c , &quot;仕切&quot; )
    ) ,
    NOT(
      ISBLANK( Margin__c )
    ),
    NOT($Profile.Name = &apos;データ移行管理者&apos;) 
)</errorConditionFormula>
    <errorMessage>「商談手数料」を入力する場合、「扱方：委嘱・協力」を選択してください。</errorMessage>
</ValidationRule>
