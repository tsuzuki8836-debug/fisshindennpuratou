<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>EstablishedCompanyCheck</fullName>
    <active>true</active>
    <description>「既設社」項目は、「工事区分」が「新設」の時以外必須とする。</description>
    <errorConditionFormula>AND(
    ISPICKVAL( EstablishedCompany__c , &quot;&quot;),
    NOT(ISPICKVAL(ConstructionDistinction__c , &quot;新設&quot;)),
    NOT($Profile.Name = &apos;データ移行管理者&apos;)
)</errorConditionFormula>
    <errorMessage>工事区分が新設以外の場合は、「既設社」項目を入力してください。</errorMessage>
</ValidationRule>
