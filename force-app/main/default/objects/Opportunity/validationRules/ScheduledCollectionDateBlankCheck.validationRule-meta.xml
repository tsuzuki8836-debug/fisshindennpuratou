<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>ScheduledCollectionDateBlankCheck</fullName>
    <active>true</active>
    <description>回収予定日は①→②→③の順番に入れる</description>
    <errorConditionFormula>AND( 
    ISBLANK( ScheduledCollectionDate2__c ) ,
    NOT( ISBLANK(  ScheduledCollectionDate3__c ) ) ,
    NOT($Profile.Name = &apos;データ移行管理者&apos;)
)</errorConditionFormula>
    <errorMessage>回収予定日は①→②→③の順番に入力してください。</errorMessage>
</ValidationRule>
