<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>CompetitorCheck</fullName>
    <active>true</active>
    <description>フェーズが受注・オーダー管理、オーダー完了の場合、受注社を入力させる。</description>
    <errorConditionFormula>AND(
    ISBLANK(Competitor__c) , 
    OR(
        ISPICKVAL(StageName, &quot;受注・オーダー管理&quot;),
        ISPICKVAL(StageName, &quot;オーダー完了&quot;)
    ),
    NOT($Profile.Name = &apos;データ移行管理者&apos;)
)</errorConditionFormula>
    <errorMessage>受注社が未入力です。</errorMessage>
</ValidationRule>
